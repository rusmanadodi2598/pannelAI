# Build output and dependencies.
.svelte-kit/
build/
node_modules/
bun.lock
package-lock.json

# Generated by the component layer's CLI (`bun x shadcn-svelte add`) and not hand-edited, so formatting it
# would be undone by the next regeneration and would produce a diff that looks like a change nobody made.
# The same exemption the Go side gives generated code (AGENTS.md §1.1). The one hand-applied change to a
# generated file (the sidebar rail width) is recorded in DESIGN.md §11.
src/lib/primitives/
